{
  "name": "@redios/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "RediOS metadata-driven mobile runtime renderer.",
  "main": "expo/AppEntry.js",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "start": "expo start",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@redios/runtime-renderer-core": "0.1.0",
    "@redios/shared": "0.1.0",
    "expo": "^56.0.9",
    "react": "^19.2.7",
    "react-native": "^0.85.3"
  }
}
